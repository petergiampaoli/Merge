// source code

const tootsiepop = {};
tootsiepop.lick = function () {
  return 'licked';
};
tootsiepop.getToCenter = function () {
  this.lick();
  this.lick();
  this.lick();
  return 'got to center';
};

const bubbleSort=(array)=>{
  swap();
  return array;
}

const swap =()=>{
  console.log(`swapped`);
}

